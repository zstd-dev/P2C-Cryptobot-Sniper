# P2C Crypto Sniper Bot

Инструмент для мониторинга и перехвата P2C-платежей в реальном времени. Работает через WebSockets и Polling одновременно, фильтрует по сумме, ведет статистику, отправляет уведомления в Telegram.

---

## Возможности

- **Гибридный мониторинг** — параллельная работа через WebSockets и Polling для минимальной задержки
- **Гибкие фильтры** — настройка минимальной и максимальной суммы (Min/Max)
- **Высокая скорость** — оптимизированные TCP-соединения, uvloop, конкурентные запросы
- **Аналитика** — ежедневные отчеты и статистика пойманных объемов
- **Безопасность** — поддержка HTTP-прокси, безопасное хранение токенов
- **Telegram-интеграция** — уведомления с QR-кодом и ссылкой, удаленные действия `complete` / `cancel` через callback

---

## Версия на C

Отдельно продается P2C-снипер на чистом C. Ловит новые платежи в реальном времени, параллельно делает `take`, автоматически реконнектится. Результат зависит от сервера, прокси и сетевых условий — инструмент продается как есть.

**Что можно приобрести:**

| Позиция | Цена |
|---|---|
| Исходный код (C) | $200 |
| Сервер + регион (пинг ~0.4 мс) | $25 |
| Список подсказок по оптимизации скорости | $25 |
| Полный пакет | $225 |

По вопросам приобретения: Telegram [@davidich444](https://t.me/davidich444)

---

## Установка

### 1. Подготовка окружения

```bash
apt update && apt upgrade -y
apt install python3-pip python3-venv git pkg-config libssl-dev -y

curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
source "$HOME/.cargo/env"
```

### 2. Клонирование и установка зависимостей

```bash
git clone https://github.com/zstd-dev/P2C-Cryptobot-Sniper
cd P2C-Cryptobot-Sniper

python3 -m venv venv
source venv/bin/activate

pip3 install -r requirements.txt
```

### 3. Первый запуск

```bash
sudo taskset -c 1 chrt -f 99 venv/bin/python3 main.py
```

После запуска откройте Telegram-бота и введите `/start` для настройки токена и лимитов.

> Запуск с высоким приоритетом процесса требует прав root.

---

## Лицензия

Программное обеспечение распространяется под гибридной лицензией.

**Copyleft.** Если вы используете бесплатный Python-код из этого репозитория и вносите в него изменения, вы обязаны опубликовать свои изменения в открытом доступе. Приватные сборки на основе свободного кода не допускаются.

**As Is.** Программное обеспечение предоставляется без каких-либо гарантий. Автор не несет ответственности за блокировки аккаунтов, финансовые потери или любые иные последствия использования.

---

## Отказ от ответственности

Данное ПО разработано исключительно в ознакомительных целях. В ряде юрисдикций автоматизация криптовалютных операций и P2C-платежей может относиться к серой или незаконной зоне. Используя этот код, вы принимаете все связанные с этим риски на себя.
